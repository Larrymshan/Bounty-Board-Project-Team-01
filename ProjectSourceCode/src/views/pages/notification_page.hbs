<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notifications</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #FFFF; /* Western yellow */
      color: #4B2E23; /* Dark brown text */
      font-family: 'Playfair Display', serif;
      margin: 0;
      padding: 0;
    }
    .notification-container {
      max-width: 800px;
      margin: 50px auto;
      background-color: rgba(244, 241, 233, 0.95); /* Semi-transparent beige */
      padding: 20px;
      border: 3px solid #8B4513; /* Saddle brown border */
      border-radius: 10px;
      box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.4);
    }
    h1 {
      text-align: center;
      color: #8B4513; /* Saddle brown */
      text-shadow: 1px 1px #4B2E23;
      text-transform: uppercase;
    }
    .notification {
      background-color: #F9F5E3; /* Light beige */
      border: 2px solid #4B2E23; /* Dark brown border */
      border-radius: 5px;
      padding: 15px;
      margin-bottom: 20px;
    }
    .notification h3 {
      margin: 0 0 10px;
      color: #8B4513;
      text-transform: uppercase;
    }
    .notification p {
      margin: 5px 0;
      color: #4B2E23;
    }
    .notification a {
      color: #8B4513;
      text-decoration: none;
      font-weight: bold;
    }
    .notification a:hover {
      color: #FFD700; /* Gold hover */
      text-decoration: underline;
    }
    .timestamp {
      font-size: 0.9rem;
      color: #6E4F3A; /* Muted brown */
      text-align: right;
    }
    p {
      text-align: center;
      color: #4B2E23;
    }
  </style>
</head>
<body>
  <div class="notification-container">
    <h1>Notifications</h1>
    {{#if recieved_notifications.length}}
      {{#each recieved_notifications}}
      <div class="notification">
        <h3>{{title}}</h3>
        <p>{{descript}}</p>
        <a href="{{link}}" target="_blank">View Details</a>
        <p class="timestamp">{{time_stamp}}</p>
      </div>
      {{/each}}
    {{else}}
      <p>You have no notifications at this time.</p>
    {{/if}}
  </div>
</body>
</html>
