<div class="container d-flex justify-content-center text-center" style="align-items: flex-start; padding-top: 20px;">
    <div>
        <h1 style="color: #8B4513; text-shadow: 1px 1px #4B2E23;">Welcome to Bounty Board</h1>
        <p style="color: #4B2E23;">The best place to get all of the tasks you don't want to do done for you!</p>
    </div>
</div>

<div class="container mt-5">
    <h1 class="text-center mb-4" style="color: #8B4513; text-shadow: 1px 1px #4B2E23;">Bounty Dashboard</h1>
    <p class="text-center mb-4" style="color: #4B2E23;">These are your active bounties you need to complete.</p>
    <a class="btn btn-primary mb-4" href="/home" style="background-color: #4B2E23; border-color: #8B4513; color: #FFF; font-weight: bold;">Back to Bounty Home Page</a>

    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark" style="background-color: #4B2E23; color: #FFF;">
                <tr>
                    <th>Job Title</th>
                    <th>Job Description</th>
                    <th>Job Price</th>
                    <th>Poster</th>
                    <th>Cancel Bounty</th>
                </tr>
            </thead>
            <tbody>
                {{#each Bounty}}
                <tr style="background-color: #FFFF; color: #4B2E23;">
                    <td>{{title}}</td>
                    <td>{{job_description}}</td>
                    <td>{{price}}</td>
                    <td>{{poster}}</td>
                    <td>
                        <form method="POST" action="/deleteBounty" class="d-inline">
                            <input type="hidden" name="BountyID" value="{{job}}">
                            {{#if is_complete}}
                            <button type="submit" class="btn btn-outline-danger btn-sm" disabled style="border-color: #8B4513; color: #B22222;">Complete</button>
                            {{else}}
                            <button type="submit" class="btn btn-outline-success btn-sm" style="border-color: #8B4513; color: #4B2E23;">Delete</button>
                            {{/if}}
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>
