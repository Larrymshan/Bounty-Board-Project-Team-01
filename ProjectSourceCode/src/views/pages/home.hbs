<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bounty Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../../resources/css/style.css">
  <style>
    body {
      background-color: #FFFF; /* Western yellow */
      color: #4B2E23; /* Dark brown text */
      font-family: 'Playfair Display', serif;
      margin: 0;
      padding: 0;
    }
    h1, h3 {
      color: #8B4513; /* Saddle brown headings */
      text-shadow: 1px 1px #4B2E23;
    }
    a.btn-primary {
      background-color: #4B2E23; /* Dark brown button */
      border-color: #8B4513; /* Saddle brown border */
      color: #FFF;
      font-weight: bold;
      text-transform: uppercase;
    }
    a.btn-primary:hover {
      background-color: #FFD700; /* Gold hover effect */
      color: #4B2E23;
    }
    .table-striped tbody tr:nth-of-type(odd) {
      background-color: #F9F5E3; /* Light beige */
    }
    .table-striped tbody tr:nth-of-type(even) {
      background-color: #F4E285; /* Western yellow */
    }
    .table-dark {
      background-color: #4B2E23;
      color: #FFF;
    }
    .table-hover tbody tr:hover {
      background-color: #FFD700; /* Gold hover effect */
      color: #4B2E23;
    }
    .btn-outline-success {
      border-color: #8B4513; /* Saddle brown border */
      color: #4B2E23;
    }
    .btn-outline-success:hover {
      background-color: #FFD700; /* Gold hover */
      color: #4B2E23;
    }
  </style>
</head>
<body>
  <div class="container d-flex justify-content-center text-center" style="align-items: flex-start; padding-top: 20px;">
    <div></div>
  </div>
  <div class="container mt-5">
    <h1 class="text-center mb-4" style="color: #8B4513; text-shadow: 1px 1px #4B2E23;">Bounty Dashboard</h1>
    <h3 class="text-center mb-4" style="color: #4B2E23;">Bounty Filters</h3>
    <div class="d-flex justify-content-center gap-3 mb-4">
      <a class="btn btn-primary" href="/activeBounties">Accepted Bounties</a>
      <a class="btn btn-primary" href="/yourCreatedBounties">Bounties Created by You</a>
      <a class="btn btn-primary" href="/completeBounties">Bounties Completed by You</a>
    </div>

    <div class="table-responsive">
      <table class="table table-striped table-hover align-middle">
        <thead class="table-dark">
          <tr>
            <th scope="col">Job Title</th>
            <th scope="col">Job Description</th>
            <th scope="col">Job Price</th>
            <th scope="col">Poster</th>
            <th scope="col">Take Bounty</th>
          </tr>
        </thead>
        <tbody>
          {{#each Bounty}}
          <tr style="background-color: #FFFF; color: #4B2E23;">
            <td>{{title}}</td>
            <td>{{job_description}}</td>
            <td>{{price}}</td>
            <td>{{poster}}</td>
            <td>
              <form method="POST" action="/home" class="d-inline">
                <input type="hidden" name="BountyID" value="{{job}}">
                {{#if is_taken}}
                <button type="submit" class="btn btn-outline-success btn-sm" disabled>Taken</button>
                {{else}}
                <button type="submit" class="btn btn-outline-success btn-sm">Take Bounty</button>
                {{/if}}
              </form>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

    {{#if Bounty.length}}
    <p class="text-muted text-end mt-3" style="color: #4B2E23;">Total Bounties: {{Bounty.length}}</p>
    {{else}}
    <p class="text-center text-muted mt-4" style="color: #4B2E23;">No bounties found. Come back later or add a bounty now!</p>
    {{/if}}
  </div>
</body>
</html>

<!-- this was a test to see why buttons were not working on table I will delete it later
<div class="ms-5 me-5">
  <table class="table">
    <thead>
      <tr>
        <th>Job Title</th>
        <th>Job Description</th>
        <th>Job Price</th>
        <th>Poster</th>
        <th>Take Bounty</th>
      </tr>
    </thead>
    <tbody>
      {{#each Bounty}}
      <tr>
        <td>{{title}}</td>
          <td>{{job_description}}</td>
          <td>{{price}}</td>
          <td>{{poster}}</td>
          <td>
          <form method="POST" action="/home">
            <input type="hidden" name="BountyID" value="{{job}}">
            <button type="submit" class="btn btn-outline-success">Add</button>
          </form>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>
-->