<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Western Theme</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #F4E285; /* Western yellow */
      color: #4B2E23; /* Dark brown text */
      font-family: 'Playfair Display', serif;
      margin: 0;
      padding: 0;
      text-align: center;
    }

    h2 {
      color: #8B4513; /* Saddle brown for headings */
      text-transform: uppercase;
      text-shadow: 2px 2px #4B2E23;
      margin-top: 20px;
    }

    .form-group label {
      font-size: 1.2rem;
      color: #4B2E23;
      font-weight: bold;
    }

    .form-control {
      border: 2px solid #4B2E23; /* Dark brown */
      border-radius: 5px;
      background-color: #f9f5e3; /* Light beige */
      color: #4B2E23;
    }

    .form-control:focus {
      border-color: #8B4513;
      box-shadow: 0 0 5px #8B4513;
    }

    .btn-primary {
      background-color: #4B2E23; /* Dark brown button */
      border: 2px solid #8B4513; /* Saddle brown border */
      color: #fff;
      font-weight: bold;
      text-transform: uppercase;
      border-radius: 5px;
    }

    .btn-primary:hover {
      background-color: #FFD700; /* Gold hover effect */
      color: #4B2E23; /* Dark brown text */
    }

    a {
      color: #8B4513; /* Saddle brown */
      font-weight: bold;
      text-decoration: none;
    }

    a:hover {
      color: #FFD700; /* Gold hover */
      text-decoration: underline;
    }

    .form-container {
      background-color: rgba(244, 241, 233, 0.95); /* Slightly transparent beige */
      padding: 20px;
      border: 3px solid #8B4513; /* Saddle brown border */
      border-radius: 10px;
      box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.4); /* Subtle shadow */
    }

    .d-flex {
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
  </style>
</head>
<body>
  <h2>Login</h2>
  <div class="d-flex">
    <div class="form-container">
      <form method="POST" action="/login">
        <div class="form-group mb-3">
          <label for="username">Email:</label>
          <input class="form-control" name="username" id="username" placeholder="username.." required />
        </div>
        <div class="form-group mb-3">
          <label for="password">Password:</label>
          <input class="form-control" name="password" type="password" id="password" placeholder="password.." required />
        </div>
        <div class="form-group mb-3">
          <button type="submit" class="btn btn-primary w-100">Login</button>
        </div>
        <p>Click <a href="/register">here</a> to register</p>
        <p>Click <a href="/resetPassword1">here</a> to reset your password</p>
      </form>
    </div>
  </div>
</body>
</html>
